# Optional — override canonical URLs (defaults to Vercel production URL, then palmeta.app locally).
# NEXT_PUBLIC_SITE_URL=https://your-project.vercel.app

# Ads stay off until AdSense is approved.
# NEXT_PUBLIC_ADS_ENABLED=false
